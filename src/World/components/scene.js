import { Color, Scene, TextureLoader, SphereGeometry, MeshPhongMaterial, Mesh, BackSide } from 'three';  

function createScene() {
  
  const scene = new Scene();
  
  return scene;
}

export { createScene };